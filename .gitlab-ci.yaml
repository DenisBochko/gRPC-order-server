stages:
  - build
  - test

variables:
  IMAGE_NAME: "order-service"
  IMAGE_TAG: "latest"

build:
  stage: build
  image: docker:latest
  services:
    - docker:dind
  script:
    - docker build -t --target builder "$IMAGE_NAME:$IMAGE_TAG" .

test:
  stage: test
  script:
    - docker build -t --target tester "$IMAGE_NAME:$IMAGE_TAG" .
